h1 todo list Food
a(href="/todos/create") add name
br
form(action="/todos", method="GET")
     input(type/text, name="q")
     button search
ul 
  each user in todo
    li
      span= user.name
      br
      a(href='/todos/' + user.id) xóa